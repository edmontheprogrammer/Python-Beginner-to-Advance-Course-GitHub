# range object is Iterable meaning we can loop over it
# String objects are also Iterable
# List is also Iterable
# You can create your own custom iterable object
for x in [1, 2, 3 , 4]:
    print(x)
